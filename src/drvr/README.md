#	README.md

#   hw

Directory Level: [ project ] / src / drvr

Driver Directory

A driver is a software component that enables communication between the software (application) and the hardware (MCU peripherals).

A driver is code that allows software to interface with hardware by providing functions for controlling hardware components (GPIO, UART, I2C, SPI, etc.).

Hardware: Includes Modules (Interface layer sub-modules, hardware abstraction layer sub-modules).... old ?

Drivers are developed and organised per the perspective of the "scope" of the core.... WIP on description....

Drivers implemented in Embedded System Firmware (DES FW), application-mode,,... are differentiated ....